const {
    GetAllRow,
    GetByIdRow,
    CreateRow,
    UpdateRow,
    DeleteByIdRow
} = require('./user.row.controller');

module.exports = {
    GetAllRow,
    GetByIdRow,
    CreateRow,
    UpdateRow,
    DeleteByIdRow
}